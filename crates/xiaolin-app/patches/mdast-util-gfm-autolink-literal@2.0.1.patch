diff --git a/lib/index.js b/lib/index.js
index c5ca771c24dd914e342f791716a822431ee32b3a..ca434296bd9fc4bf536e58d09ae78c9e8464b0d2 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -132,7 +132,7 @@ function transformGfmAutolinkLiterals(tree) {
     tree,
     [
       [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, findUrl],
-      [/(?<=^|\s|\p{P}|\p{S})([-.\w+]+)@([-\w]+(?:\.[-\w]+)+)/gu, findEmail]
+      [/(^|[\s\p{P}\p{S}])([-.\w+]+)@([-\w]+(?:\.[-\w]+)+)/gu, findEmail]
     ],
     {ignore: ['link', 'linkReference']}
   )
@@ -189,27 +189,36 @@ function findUrl(_, protocol, domain, path, match) {
 /**
  * @type {ReplaceFunction}
  * @param {string} _
+ * @param {string} separator
  * @param {string} atext
  * @param {string} label
  * @param {RegExpMatchObject} match
- * @returns {Link | false}
+ * @returns {Array<PhrasingContent> | Link | false}
  */
-function findEmail(_, atext, label, match) {
+function findEmail(_, separator, atext, label, match) {
+  // The regex uses capturing group instead of lookbehind for broader compatibility.
+  // We need to return the separator as text before the link.
   if (
-    // Not an expected previous character.
-    !previous(match, true) ||
+    // If the separator before the email is a slash, skip.
+    (separator === '/') ||
     // Label ends in not allowed character.
     /[-\d_]$/.test(label)
   ) {
     return false
   }
 
-  return {
+  const link = {
     type: 'link',
     title: null,
     url: 'mailto:' + atext + '@' + label,
     children: [{type: 'text', value: atext + '@' + label}]
   }
+
+  // Return separator as text before the link (unless at start of string)
+  if (separator && separator.length > 0) {
+    return [{type: 'text', value: separator}, link]
+  }
+  return link
 }
 
 /**
