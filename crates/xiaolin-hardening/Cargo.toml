[package]
name = "xiaolin-hardening"
version.workspace = true
edition.workspace = true
description = "Process hardening for XiaoLin: disable core dumps, ptrace, and dangerous env vars"

[lints]
workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"
