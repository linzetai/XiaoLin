apiVersion: apps/v1
kind: Deployment
metadata:
  name: xiaolin
  labels:
    app: xiaolin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xiaolin
  template:
    metadata:
      labels:
        app: xiaolin
    spec:
      containers:
        - name: xiaolin
          image: xiaolin:latest
          ports:
            - containerPort: 18789
              name: http
          env:
            - name: XIAOLIN_PORT
              value: "18789"
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /etc/xiaolin
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: xiaolin-data
        - name: config
          configMap:
            name: xiaolin-config
---
apiVersion: v1
kind: Service
metadata:
  name: xiaolin
  labels:
    app: xiaolin
spec:
  type: ClusterIP
  ports:
    - port: 18789
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: xiaolin
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xiaolin-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
