# fastclaw-msteams

Microsoft Teams / Bot Framework 渠道扩展。

## 功能

- **OAuth 认证** — Client Credentials 自动获取 token
- **Activity 接收** — Bot Framework Webhook activity 解析
- **消息发送** — Activity POST 消息发送与回复

## 关键导出

```rust
pub use plugin::{TeamsPlugin, TeamsPluginConfig};
```
